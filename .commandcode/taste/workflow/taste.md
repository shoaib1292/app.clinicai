# workflow
- Before any destructive action (deleting files, removing folders, stripping code), first produce a documented plan and get explicit approval. Never delete without confirmation, even during cleanup/audit tasks. Confidence: 0.70
- When running ad-hoc Node scripts against the project (DB migrations, data backfills), write them as a temp `.cjs` file under the project's `scripts/` folder and run with `node` — don't inline long one-liners in `node -e` (shell escaping, especially `# workflow
 on Windows cmd, breaks them) and don't put them in the scratchpad (they can't resolve the project's `node_modules`). Delete the temp script after execution. Confidence: 0.80
- When the user asks to enable a mandatory gate (e.g., email verification) but allow current/test accounts to keep working, first audit the existing enforcement (signup/login/verify/resend routes) and backfill the existing records via a DB update before touching anything else — only then confirm the new-signup path is complete end-to-end. Confidence: 0.65
- When the user asks for a feature or change, first explore the relevant existing codebase thoroughly (current auth flows, data models, session handling, existing but unused infrastructure), then present a structured analysis with a clear recommended strategy and ask for confirmation — before writing any implementation code. Confidence: 0.75
- When the user asks whether a third-party service is easy to integrate ("kia tum X integrate kr do gy?", "proper add karna aasan hai?"), treat it as a feasibility question: inspect their actual codebase (schema, booking flow, portal, dashboard, auth patterns) and answer with a concrete effort estimate — days/sessions, files touched, task breakdown, and honest risk points (e.g., TURN/NAT for WebRTC) — plus an explicit step-by-step implementation offer. "Proper" integration means production-ready (webhooks, billing, edge cases), not a demo. Confidence: 0.6
- When the user asks a founder-level technology choice question ("X vs Y, kia best hai as a founder"), give a definitive, well-reasoned recommendation with concrete reasoning — not a neutral feature comparison. The user wants the assistant to take a stance, justify it with data (pricing, limits, trade-offs), and provide a phased strategy for when to revisit the choice at scale. The user often frames these as "long term for ClinicAI" — so factor scalability, upgrade paths, and open-source/self-host escape hatches into the recommendation, not just today's cheapest option. Confidence: 0.75
- When integrating a new third-party service or API (e.g., Daily.co, Agora), thoroughly read the external documentation first before writing any implementation code — don't guess or rely on training knowledge alone. Confidence: 0.85
- When the user references an external product as a UX/design benchmark and explicitly asks to research it ("GHL jesi banai hai... research kar lo"), web-search the reference product's actual UI patterns before building — don't rely on memory or guesswork. The user expects the assistant to empirically verify how the reference product does it, then replicate that interaction pattern faithfully. This applies to any product the user names as a benchmark, not just GHL. Confidence: 0.75
- When consolidating multiple duplicate/overlapping documents (analysis reports, plans), create a single merged reference .md file first before removing the originals. Confidence: 0.65
- Current deployment priority: web app (app.clinicai.pk) and landing page (clinicai.pk) first, mobile apps deferred until web is live. Focus on testing, polishing, and deploying the web experience within a tight 2-3 day timeline. Confidence: 0.70
- Prefer managing LLM API keys through the Platform Admin → LLM Keys UI rather than environment variables or local config files. Confidence: 0.80
- Every Platform Admin key/credential management page must include delete functionality — not just add and list. The user wants to prune non-working keys and keep the list clean ("ta k working api key hi rakhi jayen"). Full CRUD is the expectation for any credential management UI. Confidence: 0.80
- When a code fix doesn't resolve an issue, verify at runtime by checking server status, logs, DB state, and external API connectivity — don't just re-read code or assume the fix worked. Confidence: 0.85
- When the user reports "nothing changed" after UI edits (or reports a UI bug that the current code cannot possibly produce, e.g., an old button/title overlap), the usual culprit is stale code being served — either browser cache on the tunneled URL (app.clinicai.pk) or a dev server that was started BEFORE the edits and keeps serving the old compiled bundle. Verify by grepping the source for the reported old strings, searching .next chunks for the new strings, and checking the dev server's start time vs. edit time; only then tell the user to hard refresh (Ctrl+Shift+R), use incognito, or restart the dev server — rather than assuming the code is wrong and redoing it. Confidence: 0.80
- When a UI change/feature isn't visible in the browser, confirm the running server is actually in dev mode by checking the process command line (e.g., `netstat -ano` to find the PID on the port, then `wmic process where "ProcessId=<pid>" get CommandLine`): `next dev` hot-reloads file changes, but a production server (`next start` via `start-server.js` after a build) serves the old compiled bundle and never picks up edits, even when the port matches. Distinguishing dev vs. production mode from the process command line is a required step before blaming the code, the cache, or the browser. Confidence: 0.70
- When a Next.js hydration error trace shows event handlers or props that do not exist in the current source file (e.g., `onClick={function onClick}`, `onInput={function onInput}` in a component that only defines `onBlur`), the dev server is conclusively serving a stale cached build — don't iteratively rewrite the code. Clear `.next` and restart the dev server; if `.next` can't be deleted (locked by running process), instruct the user to kill and restart the dev server manually. Compare the error trace's rendered props against the actual source to distinguish "stale cache" from "code bug" instantly. Confidence: 0.80
- Check actual runtime configuration of external services (e.g., webhook settings on Evolution instances) via API calls rather than only inspecting code — the real root cause may be a runtime config issue, not a code bug. Confidence: 0.80
- Test external API endpoint behavior with real HTTP requests (e.g., PowerShell Invoke-WebRequest) before implementing fixes, to verify the correct request format and parameters empirically rather than guessing from code alone. Confidence: 0.80
- When integrating external services (Evolution API, Meta Cloud API, etc.) into the clinic onboarding flow, the setup must be fully automatic — the clinic admin should never have to manually configure webhooks, enable events, or touch the external service's dashboard. Every configurable parameter must be set programmatically at instance creation time so the experience is "connect and it just works". If the REST API doesn't support it, find the correct API format empirically rather than punting to manual setup. Confidence: 0.80
- When the AI agent stops responding to messages, systematically debug by: checking if the server is running (netstat), testing the webhook endpoint with a real HTTP request, checking DB for message state, checking server logs, identifying stale server processes, killing old PIDs, restarting the server, waiting for boot, and re-testing. Confidence: 0.80
- When STT/transcription fails or returns an unsupported language, the AI agent should respond in Urdu asking the user to speak in Urdu or send a text message, rather than showing a technical error or generic failure. Confidence: 0.85
- When restarting the dev server, kill the old process by PID first to ensure stale code is cleared, then start fresh in background and wait for full Next.js boot before sending test requests. Confidence: 0.75
- When running Next.js dev on Windows, Turbopack native bindings are not available at all (win32/x64) — always use `next dev` without `--turbo` (which defaults to Webpack). This is not just a background-job issue; Turbopack is fundamentally unsupported on this platform. Confidence: 0.90
- When a command or tool hangs/stalls (e.g., shell_command timing out or being repeatedly interrupted), immediately pivot to an alternative approach (PowerShell, different tool, direct API call) rather than waiting or retrying the same method. The user values fast, unstuck execution and gets frustrated by visible delays. Confidence: 0.85
- When a CLI command misparses flags (wrong transport type, flags interpreted as positional args, especially on Windows where shell quoting differs from Unix), don't repeatedly tweak flag orderings — after one or two attempts, skip the CLI entirely and directly edit the underlying config file (JSON/YAML). The config file is the source of truth and bypasses CLI parsing edge cases. Always verify the fix with the CLI's list/status command afterward. Confidence: 0.80
- When the user provides MCP server credentials (URL + token), they expect the assistant to register the server into its own configuration itself — not to give the user terminal commands to run manually. The user says "khud main add karo" meaning "add it to yourself." Confidence: 0.75
- When building a new mobile app, build the complete frontend/UI first with mock/static data before wiring any backend — so the user can visually test the app on their device (Expo Go) early, without waiting for API integration. Confidence: 0.85
- When the user asks forward-looking questions about long-term workflow, capabilities, or roadmap ("aagy kia karon ga", "kis had tak help karo gy"), provide a transparent, structured breakdown covering: (a) daily operational workflow after go-live, (b) what the assistant can help build (with concrete feature categories), and (c) what the assistant explicitly cannot do (with reasons why). Be honest about limitations rather than over-promising — the user values knowing boundaries upfront. Confidence: 0.75
- Skills installed via `npx -y skills add --agent claude-code` land in `.claude/skills/` by default, but Command Code only detects skills from `.commandcode/skills/`. After installing skills for Claude Code, also copy them to `.commandcode/skills/` (on Windows use `xcopy /E /I /Y`) so Command Code can use them too. The user expects skills to work in both environments. Confidence: 0.75
- After installing new skills, always provide a brief table explaining how to use each skill (e.g., `/skill-name` for what purpose) — the user wants practical usage guidance, not just installation confirmation. Confidence: 0.70
- When the user asks for a plan ("short sa plan btao"), keep plans concise and high-level with only the key structure — not exhaustive multi-page documents. The user prefers a quick overview they can scan and approve. Confidence: 0.70
- When delivering code (especially mobile apps), self-verify by actually running the app, fixing all runtime errors, and only report "done" when it genuinely starts without errors. Never hand off untested work — the user gets frustrated when told something works only to hit errors immediately on their end. Confidence: 0.90
- When killing processes after verification, be surgical — kill only the specific command/process that was started for testing (e.g., just the Expo dev server), not other running services or background tasks. Confidence: 0.70
- When the assistant gets stuck on mobile app development (repeated errors, dependency issues, runtime crashes), pivot to a handoff: document the backend API in a comprehensive `.md` reference file (endpoints, request/response JSON, auth flow, screen-to-API mapping, design rules), and let the user build the frontend in Google AI Studio — they will come back for backend integration. The user views Google AI Studio as their reliable mobile frontend builder and prefers splitting work this way rather than having the assistant keep fighting mobile tooling issues. Confidence: 0.85
- Once a handoff/reference document has been created for another tool (e.g., a `.md` file for Google AI Studio), keep it stable and communicate supplementary information directly in chat — don't edit the handoff document. The user treats these as clean artifacts for a specific recipient and doesn't want them polluted with follow-up additions. Confidence: 0.65
- When the user reports multiple errors at once, diagnose all of them in parallel first (simultaneous file reads, grep searches, schema checks), create a structured fix plan (todo list), apply all fixes together, then verify — rather than fixing errors one at a time sequentially. The user expects the full error surface to be resolved in a single efficient pass. Confidence: 0.80
- After making code edits, provide a transparent summary of exactly which files were touched (e.g., `git diff --stat HEAD`) so the user can verify their existing uncommitted work wasn't disrupted. The user often has significant uncommitted changes and gets anxious when they think work may have been overwritten. Confidence: 0.65
- Stay focused on the current feature/task until it is genuinely complete — do not proactively pivot to a new topic or workstream (e.g., reading design docs, activating skills for unrelated work) while active development is still in progress. The user will explicitly direct the next priority when ready. Confidence: 0.85
- When development tools, system packages, or utilities (Python, CLIs, SDKs) are needed for the task, install them without asking for permission — the user expects the assistant to be self-sufficient for tooling setup. Only consult on substantive architecture/product decisions, not tool installation. Confidence: 0.75
- When the user says "kaam shuru karo step by step", implement in clearly communicated phases with todo tracking — don't try to do everything in one monolithic pass. The user wants visible, incremental progress they can follow. Confidence: 0.80
- Stay in planning mode — iterate on the plan document only — until the user explicitly says to start implementing. During planning, update the plan file based on feedback but do NOT write any implementation code. The user will give an explicit signal (e.g., "kaam shuru karo") when ready. Confidence: 0.85
- When the user references a design file with `@filename.md` notation (e.g., `@block.md`), that file is the creative brief — it contains specific visual concepts and design mockups that should be faithfully reproduced in implementation, not loosely interpreted. Search for these files in the project root and treat them as first-class design specs alongside the plan document. Confidence: 0.75
- When fixing a structural/layout issue on a page (missing sidebar, broken shell, wrong parent wrapper), first read an existing working page of the same category/section to see the correct structural pattern, then apply it to the broken page — don't guess the wrapper/props from memory. The codebase has consistent patterns (e.g., DashboardShell wrapping, session→redirect flow) that should be copied verbatim from sibling pages. Also sweep the rest of the section with grep to confirm no other pages share the same missing-wrapper bug before declaring the fix complete. Confidence: 0.85
- When a server component already has session data (clinicId, userId, userName) from `getSession()`, pass it as props to child client components rather than having the client fetch `/api/auth/me` again. This avoids redundant network round-trips and keeps the data flow unidirectional. Confidence: 0.75
- When a Next.js route 404s despite the route file existing at the correct path, check the middleware/proxy layer (`proxy.ts`/`middleware.ts`) for incorrect rewrites before debugging route registration or file structure. Domain-based middleware that rewrites all traffic can silently redirect valid routes to non-existent paths (e.g., `/p/al-shifa` → `/website/al-shifa/p/al-shifa`). Confidence: 0.75
- When a Next.js dynamic route 404s despite the route file and middleware both being correct, the root cause is often a data-layer filter — the route handler queries the DB with a boolean/conditional WHERE clause (e.g., `websiteEnabled: true`, `published: true`) that the seed script or DB state doesn't satisfy. Inspect the DB directly (e.g., `docker exec ... psql`) to verify the relevant flag before rearchitecting the route. Confidence: 0.75
- When an outward-facing link "doesn't open" (404) — referral links, booking links, any URL the app generates and copies for users — trace where the URL is generated/copied in the UI and diff it against the actual Next.js route signature (folder/param structure). A common root cause is a generated link missing a required path segment (e.g., copy button producing `/r/{slug}` while the route requires `/r/{slug}/{code}`), which is distinct from middleware rewrites or data-layer filters. Confidence: 0.6
- Before adding directories to tsconfig.json `exclude`, verify no source files import from those directories — excluding an imported directory causes hard-to-diagnose module resolution errors. Check with grep rather than assuming directories are isolated. Confidence: 0.70
- During active development/testing phases, hardcode auth/security values (OTPs, verification codes, magic links) to simple test constants so testing is frictionless — the real logic will be restored before production. Mark temporary code with `// TEMP:` comments so it's easy to find and revert later. Confidence: 0.75
- When the user reports a feature isn't working at a high level ("booking nahi ho rahi", "data nahi aata"), explore the entire feature flow end-to-end first — all related pages, all API routes, and all data models — before diagnosing or fixing. The root cause is often spread across multiple files and a narrow investigation misses the full picture. Confidence: 0.75
- When the user sends a screenshot alongside any problem report — not just error messages but also silent failures, forms that won't submit, or unexpected UI states — use `vision` to read the exact screen state as the first diagnostic step. Don't rely solely on the user's text description; the visual state often reveals details (e.g., which fields are visible, whether a form rendered at all, what buttons are present) that the user didn't mention and that drive the correct diagnosis. Confidence: 0.80
- When the user says a tool "doesn't work" without specifics (e.g., "docker desktop install to karta hon lekin sahih work nhi krta"), investigate the installation status and dependencies (Is it installed? Are prerequisites like WSL present?) before offering alternatives. Don't jump straight to workarounds — confirm the problem state first, then present a clear pivot with a recommendation. Confidence: 0.70
- When the user sends a screenshot of an error message, use `vision` to read the exact error text before diagnosing — don't guess the error from visual description alone. The exact error code/message often contains the root cause (e.g., "Virtualization support not detected") that drives the correct diagnosis. Confidence: 0.70
- When the user sends a screenshot and claims "same issue" after a fix was applied, use `vision` to independently verify what's actually on screen — don't assume the user's problem classification is correct. The visual state may reveal a completely different root cause (e.g., form not rendering at all vs. a React console warning), and chasing the wrong diagnosis wastes iterations. The user describes symptoms; vision reveals the real state. Confidence: 0.70
- When a file has accumulated multiple bugs or structural issues (JSX mismatches, wrong Prisma fields, bad function signatures), especially after several incremental `edit_file` calls have compounded errors, prefer a clean wholesale rewrite (via `write_file` or a single large `edit_file`) over further incremental fixes. Use `edit_file` only for single isolated fixes. Repeated small edits on a broken structure risk accumulating more errors and frustrating the user. Confidence: 0.80
- When removing a component's internal `DashboardShell` wrapper so the page-level wrapper can own it, also check and update every page that renders that component, plus the sibling-page wrapper pattern, in one pass — the component is often rendered from multiple routes (e.g., two template routes), and the removal must be consistent across all of them. Additionally, expect the unwrap to change prop usage (an unused `session` prop must be cleaned from both the client component and every caller) and to expose brace/JSX-structure imbalance from the removed wrapper — verify and fix the full structure before declaring done. Confidence: 0.75
- After an `npx tsc --noEmit` run dies with an out-of-memory error instead of type diagnostics, don't interpret it as a type error — first retry the full check with a larger heap (`set NODE_OPTIONS=--max-old-space-size=8192 && npx tsc --noEmit` on Windows), which often lets the whole project typecheck complete. If that still fails, pivot to a targeted check (transpile/parse just the touched file or filter `tsc` output to it) to confirm syntax, and treat the OOM as an environment limitation, not evidence of broken code. Confidence: 0.65
- When a data-layer fix makes a previously-unreachable code path reachable (e.g., toggling a boolean that was blocking a page), proactively read the rendering code to verify the edge case revealed by the fix — if the page now renders but has no data/blocks/config, does it crash or degrade gracefully? Don't just verify the immediate fix; verify the full render path the user will experience next. Confidence: 0.65
- When the user reports a feature still isn't working after a previous fix was claimed "done," start by re-reading the current file state — the previous work may have been incomplete (e.g., types/helpers added but never wired to UI state or save logic) rather than broken by subsequent changes. Don't assume the earlier work was complete just because it was marked as done. Confidence: 0.75

- When the user sends a screenshot of a settings panel, API playground, or configuration UI showing slider positions/values/selections (e.g., TTS playground with speed/rhythm sliders), use `vision` to read the exact configuration, then empirically test those parameter values against the live API (via curl) before implementing in code. The user discovers optimal configurations through visual playgrounds and communicates them via screenshots rather than describing parameter names and values in text. Confidence: 0.80
- After writing or rewriting API route files, always run `npx tsc --noEmit` filtered for the changed files to catch type errors before reporting completion — don't assume rewritten code is type-correct just because the logic looks right. Catching Prisma field mismatches and function signature errors immediately saves back-and-forth. Confidence: 0.75
- When Next.js throws a "module factory is not available" error (especially for lazy-loaded icon/component libraries like lucide-react), clear `.next` cache directory and check for dead imports — the stale chunk cache can reference modules that are no longer used, and the error persists until both the cache is cleared AND the unused imports are removed. Confidence: 0.75
- Prefers quality over quantity: individual features must be polished with proper design feel rather than mass-producing many functional-but-bland blocks. When implementing from a design spec file (like block.md), first discuss each component's visual flow and design intent collaboratively before writing any code — never skip the design conversation and jump straight to functional implementation. The user values craftsmanship over throughput. Confidence: 0.85
- When reorganizing sidebar/navigation, never remove existing links — the goal of reorganization is to move, regroup, and reprioritize items, not to delete them. Removing nav entries breaks existing flows and makes features inaccessible (the user calls this "ghaib karna" — making things disappear). After any navigation restructure, verify every previously-accessible page is still reachable from the sidebar. Confidence: 0.85
- Before recommending or sizing any self-hosted service (LiveKit, video SFUs, mail servers), read the official self-hosting/deployment documentation for sizing guidance (CPU, RAM, bandwidth per participant/concurrent call) and ground the capacity math in those numbers rather than estimating from general knowledge. Confidence: 0.70
- When a UI crash is caused by missing/undefined data from an API response, apply a two-layer fix: (a) add optional chaining/nullish coalescing at the call site for immediate crash prevention, then (b) fix the API query (Prisma include/select) to return the missing data. Don't stop at just the defensive frontend patch — the proper fix is at the data layer. Confidence: 0.75
- When fixing a missing-export build error, also inspect all other imports from that same module in the importing file — the build-crashing import is just the first one TypeScript detected, and adjacent imports may also have signature mismatches (e.g., wrong number of arguments, renamed functions). Fix the full import surface in one pass rather than letting the next build error surface the next mismatch. Confidence: 0.70

- When a module import resolves to the wrong code despite the intended file being present and correct, suspect file-vs-directory name collision: if both `foo.ts` and `foo/index.ts` exist, Node.js/TypeScript resolves bare imports (`import ... from '@/path/foo'`) to the file over the directory. This silently shadows the new/intended module with old legacy code. When a fix seems ineffective but config and code both look correct, grep for sibling files that share the same base name as the import target. Confidence: 0.80

- When the user says "continue" mid-investigation, they're signaling to keep going with the current workstream — don't wait for more input or ask clarifying questions. Just proceed with whatever the next logical step is. Confidence: 0.60
- When starting a large feature that touches multiple codebase areas (schema, routes, UI patterns, auth), launch multiple parallel sub-agent `explore` calls to investigate different aspects simultaneously rather than reading files sequentially. This cuts discovery time dramatically — 3 parallel agents exploring schema/fees, booking routes, and dashboard patterns at once instead of 3 sequential read passes. Confidence: 0.85
- Before building a new feature that depends on existing infrastructure, fix any naming mismatches, hardcoded values, or missing exports in the foundation code first — don't build on broken foundations. The new feature should land on clean, correct infrastructure. Confidence: 0.80
- When `prisma migrate dev` stalls or fails in a non-interactive environment, skip it and push schema changes directly via `prisma db push --accept-data-loss --skip-generate` followed by `prisma generate` — don't keep retrying the interactive migration command. The migration file can be created later when a proper terminal is available. Confidence: 0.80
- When waiting for a long-running background task (build, install, dependency generation), never use blocking wait patterns (`block: true` with long timeouts) — the assistant appears frozen/stuck and the user gets frustrated. Instead, use background execution (`run_in_background: true`) and poll results asynchronously, or find a synchronous alternative that completes quickly. Confidence: 0.85
- When an external API call fails with an auth/format error (invalid key, wrong response shape, 401), do NOT report the failure to the user after the first attempt. Instead, exhaustively try alternative request formats first: JSON body vs multipart form-data, different auth header styles (X-API-Key vs Authorization: Bearer vs query param), different endpoint paths (/v1/stt/transcribe vs /api/transcribe), different model/parameter names, and different key placements. Only report "the key is invalid" or "it doesn't work" after genuinely exhausting format variations — the user expects the assistant to figure out the correct API contract empirically rather than giving up at the first error. Confidence: 0.80
- When the user explicitly reports they tried the suggested diagnostic step (restart, cache clear, dev server restart, refresh, etc.) and the problem persists, accept their report at face value and immediately pivot to a deeper/code-level investigation — don't re-suggest the same fix or question whether they did it correctly. The user's diagnostic feedback is reliable input; when they say "abhi bhi nhi aaya refresh bhi kia," the issue is genuinely in the code, not the cache. Confidence: 0.75
- Before writing code that references a new Prisma model (e.g., `db.pharmacist.findFirst()`), verify the model actually exists in `prisma/schema.prisma` first — don't assume it's there just because it was mentioned in conversation or seems logical. If the model doesn't exist, remove all references to prevent runtime `TypeError: Cannot read properties of undefined` crashes. Confidence: 0.80

- When the user asks whether the project is production-ready ("is this live hone ke qabil hai?"), perform a systematic multi-dimensional audit before answering — check deployment config, security (hardcoded credentials, bypassed auth), infrastructure (service health, provisioning), configuration (env vars, external API keys), and code quality (TODOs, incomplete pages). Categorize findings by severity (critical blockers vs. gaps) and provide a concrete time estimate. Don't give an offhand opinion based on general familiarity with the codebase. Confidence: 0.65
- Never run a command that would kill the assistant's own Node.js process (e.g., `Get-Process -Name "node" | Stop-Process -Force`). When such a command is needed, give it to the user to run manually in their own terminal instead. The user has explicitly complained about this pattern ("tum khud ko hi band kar dete ho"). Confidence: 0.80
- Handle all git operations for the user — repo setup, .gitignore, staging, committing, pushing. The user is not a git expert and doesn't want to be involved in git details; they just want clean code on GitHub so they can `git clone`/`git pull` to their VPS. Don't ask the user to make git decisions they don't understand — just do the right thing without burdening them. Confidence: 0.80
- When giving configuration instructions (IPs, ports, API keys, URLs), verify the actual runtime values on the user's system and provide exact, copy-pasteable values — not examples with "replace this with your own" instructions. The user tends to copy example values literally even when told to substitute them. When the user reports a connection failure and says "ye btao kia sahi hai" (just tell me what's correct), they want a definitive verified answer, not a debugging walkthrough. Confidence: 0.60
- Use structured commit messages with a version tag prefix (e.g., `v0.1.0:`), a descriptive title line, blank line, bullet-point feature/change summary, blank line, and `Co-authored-by: CommandCodeBot <noreply@commandcode.ai>` footer. This format gives the commit a professional, release-note quality. Confidence: 0.75
- Clean up temporary git-related files (e.g., `.commit-msg.txt`) after the git operation completes — don't leave scratch files in the repo root. Confidence: 0.65
- When the user independently resolves a platform/infrastructure issue (like Docker setup, WSL configuration) and announces it's ready, verify the tool actually works, then proceed directly to integration work — don't reconsider or re-argue alternative approaches (e.g., "are you sure you don't want native install instead?"). The user wants to move forward, not re-litigate solved problems. Confidence: 0.70

- After adding or modifying Prisma models in `schema.prisma`, always follow up with `prisma db push` to sync the local database — don't leave schema changes as code-only. The user expects the DB to be updated as part of the same implementation pass and shouldn't have to ask "db bhi push kar do" separately. Confidence: 0.80

- When finishing a feature domain that involves external third-party console/API setup (Google Cloud Console, Meta Developer, Stripe, etc.), the user expects a comprehensive setup guide saved as a markdown file in the project root (e.g., `GOOGLE_SETUP_GUIDE.md`). The guide must include: step-by-step console configuration with direct URLs, all required env vars with descriptions AND how to obtain each value (not just placeholder names), which APIs/services to enable, local testing instructions, production deployment notes, and a summary of what features auto-activate after setup. The user explicitly asks "konsi chezen .env me dalni hain... kese leni hai pora process bhi btao" — they want an end-to-end operational document, not just a list of variables. Confidence: 0.80

- When the user reports that a setup guide or instructions don't match what they actually see on a third-party console (Google Cloud Console, Meta Developer, Stripe, etc.), immediately web-search for the latest UI/API surface and rewrite the guide to match current reality — don't defend the old guide, ask the user to figure it out themselves, or give generic "look for something similar" advice. The user expects the guide to be accurate to the current console experience, and third-party UIs change frequently. Confidence: 0.80
- When the user describes a desired end-to-end experience spanning multiple services in a pipeline (e.g., voice → STT → Agent → TTS → WhatsApp delivery), fix and verify the complete chain, not just the explicitly broken component. Check upstream data flow (is the webhook/provider sending all needed fields?) and downstream processing (is the output format correct for the next stage?) alongside the core fix. The user thinks in terms of complete user journeys, not individual API calls. Confidence: 0.80

- When the AI agent produces correct responses for text input but poor/generic responses for voice input (or any other modality mismatch), the root cause is in the modality-specific pipeline (webhook audio parsing, STT transcription quality, audio download/encoding) — not the shared agent/prompt logic. If text works fine, the agent and prompt are innocent; investigate the voice pipeline end-to-end before touching shared code. Confidence: 0.80

- When a webhook provider's payload format is inconsistent (some fields may be omitted while others are present — e.g., Evolution API sending only `url` without `base64` for voice notes), always implement a fallback path that fetches the missing data via a separate API call rather than silently proceeding with incomplete data or failing. Never assume all fields will be present in every webhook delivery. Confidence: 0.80
- When multiple related pieces of state are saved together (e.g., blocks + fonts, or branding + theme), merge them into a single unified `save()` call rather than calling save separately for each concern. Separate calls each trigger their own API request, their own toast notification, and their own success/failure path — leading to duplicate toasts, race conditions, and inconsistent state when one succeeds but another fails. Extend the save function signature with optional params and consolidate all mutations into one PATCH. Confidence: 0.80
- When rapid async events can fire concurrently (e.g., `onBlur` on two contentEditable fields in quick succession), guard the async operation with a `useRef` boolean gate — not just `useState`. A ref is synchronously read/written and doesn't cause re-renders, making it the correct primitive for preventing duplicate API calls. The `useState` handles the loading indicator; the `useRef` prevents the actual duplicate work. Check-and-return early: `if (savingRef.current) return; savingRef.current = true` before the async call, reset to `false` in finally. Confidence: 0.75

- When a Coolify deployment fails at the Docker image export/push layer but the build step itself succeeded (TypeScript compiled, static pages generated, Docker build completed), the root cause is likely a transient infrastructure/resource issue, not a code bug — redeploy immediately rather than debugging code. Only investigate code changes if the build/compile step itself fails. Confidence: 0.70
- When adding a new Docker service alongside an existing project (e.g., n8n for automation), use a separate docker-compose file (e.g., `docker-compose.n8n.yml`) rather than modifying the main `docker-compose.yml` — this keeps services independent, avoids accidental interference, and lets each service be started/stopped independently. Confidence: 0.65
- Never kill, restart, or interfere with a dev server already running on the project's port (e.g., port 3000) — it is the user's live environment. Verify process ownership first (netstat + tasklist) and don't start competing servers on the same port; if a server is already running, assume the user's edits will hot-reload into it and avoid hitting it with test requests. Confidence: 0.7
- When verifying after a change, distinguish pre-existing errors from errors in the files you touched — filter `tsc --noEmit` / `eslint` output to the changed files and get those clean, rather than chasing unrelated pre-existing errors (missing deps, seed files, untouched modules). Only fix pre-existing errors in files the task actually requires touching. Confidence: 0.7
- When the user highlights/annotates a region in a screenshot (e.g., blue highlight) and instructs a change ("ye remove kr do"), the annotation is a precise change directive — use `vision` to identify exactly which element is highlighted, scope the change to that element only, and explicitly preserve any elements the user calls out (e.g., "logo nhi hatana clinicai ka"). Don't over-delete adjacent UI. Confidence: 0.8

- When a Prisma runtime error (PrismaClientValidationError) reveals a field that doesn't exist in the schema (a "ghost field" like `extraClinicFee`), first grep `prisma/schema.prisma` to identify the real field names (`clinicMarkup`, `markupMin`/`markupMax`, `clinicMarkupTotal`), then sweep the entire codebase for every reference to the ghost field — server-page selects, client interfaces, fee displays, API `_sum`/aggregate calls, and form fields — and fix them all in one pass. Don't stop at the reported crash site: the same ghost field silently breaks sibling pages or renders misleading 0 values elsewhere (public booking, analytics, invoices, pricing rules). Client interfaces referencing the ghost field type-check fine but produce wrong output at runtime, so they need fixing too. Confidence: 0.8

- When the user asks whether an existing feature actually has a real use case or is broken ("koi proper use case hai bhi... ya code broken hai?"), trace the feature's actual usage end-to-end first (grep for where its data/fields are read at runtime and follow the real send/processing path) and answer with a clear verdict that distinguishes "broken" (crashes/compile errors) from "dead/incomplete" (wired to UI + DB but never connected to the real flow, e.g., hardcoded strings bypassing the feature's table). The user wants to know whether a feature is genuinely functional or just a showpiece — not merely whether it compiles — and whether to wire it up or remove it. Confidence: 0.6

- When the user approves removing a dead/incomplete feature ("remove kr do"), remove it comprehensively across every surface in one pass: main nav entries AND the section-grouping label lists, keyboard-shortcut entries, breadcrumb route-label maps, the page files/directories, and the API routes. Before deleting the API routes, grep for any other caller of `/api/<feature>`. After removal, grep the whole `src` tree for the feature's route paths/component names to confirm zero dangling references remain. Keep shared data/tables that still-live features consume (e.g., a `NotificationTemplate` table still read by Meta sync and Reminders display) — only strip the dead UI/routes. Half-removal (link gone but pages/routes still shipped) is considered incomplete. Confidence: 0.7

- When the user says "sirf batao koi changes nhi karni" (just tell me, don't make any changes), treat the request as assessment-only: give the analysis/opinion and make NO code edits, config changes, or deletions. The user is seeking a status/readiness verdict, not an implementation directive. Confidence: 0.75

- When the user asks a founder-level readiness question ("founder level batao", "end-to-end system ban gaya hai?"), give a candid verdict that explicitly separates "built/code-complete" from "battle-tested/production-ready" — first acknowledge what's genuinely solid, then list the real risks in priority order, and end with a clear bottom-line answer. Include non-code risks too: legal/compliance claims (e.g., "no tax" is a business claim code cannot guarantee — recommend validating with a CA/lawyer), PHI/data-privacy consistency, unproven external integrations, and dependencies on separate repos. Don't equate "typecheck passes + tests pass" with production readiness. Confidence: 0.7
- ClinicAI versioning scheme: launch at v0.0.4, then patch within the 0.0.x line (up to 0.0.99) before bumping the minor version to 0.1.0 — a disciplined semantic-versioning progression rather than arbitrary version jumps. Confidence: 0.7
- Deployment workflow: push clean code to git (never commit fake/seed data or `.env` files), deploy via Coolify on a VPS running PostgreSQL, Redis, and any other required services, then integrate and iterate with continuous updates. Confidence: 0.8
- Product strategy: publish, serve a real business, collect user reviews, and improve based on feedback — ship to real users and iterate on real feedback rather than polishing indefinitely in isolation. Confidence: 0.7
