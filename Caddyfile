# ClinicAI Caddy Reverse Proxy
# Catch-all for clinic custom domains + wildcard subdomains
# Proxies ALL traffic to the Next.js app, which handles clinic routing via proxy.ts

:443 {
  tls {
    on_demand
  }
  reverse_proxy app:3000
}

:80 {
  reverse_proxy app:3000
}
